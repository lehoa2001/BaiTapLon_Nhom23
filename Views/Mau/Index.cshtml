@model IEnumerable<BaiTapLon_Nhom23.Models.Mau>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenMau)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenMau)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaMau">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaMau">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaMau">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
