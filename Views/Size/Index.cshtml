@model IEnumerable<BaiTapLon_Nhom23.Models.Size>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenSize)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenSize)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaSize">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaSize">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaSize">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
